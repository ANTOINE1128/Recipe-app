
  <% @recipes.each do |recipe| %>

 
        <section>
          <%= link_to recipe_path(recipe) do %>
            <p><%= recipe.name %></p>
          <% end %>
          <%= button_to  "Delete", recipe_path(recipe), method: :delete, class: 'link' %>
        </section>
        <section>
          <p><%= recipe.description %></p>
        </section>
    </div>
  <% end %>
  <%= link_to 'Add Recipe', new_recipe_path%>
</div>
